.home {
    margin: 2rem 0 7rem;
    line-height: 1.4;

    &__title {
        font-size: 1.4rem;
        margin-bottom: 1.2rem;
        font-weight: bold;
    }

    p {
        margin: 0.8rem 0;
    }

    img {
        display: block;
        margin: 1.2rem auto 3rem;
        max-width: 100%;
        height: auto;
    }

    strong {
        font-weight: bold;
    }
}