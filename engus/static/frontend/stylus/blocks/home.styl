.home {
    margin: 2rem 0;
    line-height: 1.4;

    &__title {
        font-size: 1.4rem;
        margin-bottom: 1.2rem;
        font-weight: bold;
    }

    p {
        margin: 0.8rem 0;
    }

    strong {
        font-weight: bold;
    }
}